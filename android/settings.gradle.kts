// Standalone Android build — deliberately decoupled from backend/ Gradle (ADR-0012):
// AGP pins its own Gradle matrix and CI builds each subproject independently (ADR-0010).
pluginManagement {
	repositories {
		google {
			content {
				includeGroupByRegex("com\\.android.*")
				includeGroupByRegex("com\\.google.*")
				includeGroupByRegex("androidx.*")
			}
		}
		mavenCentral()
		gradlePluginPortal()
	}
}
dependencyResolutionManagement {
	repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
	repositories {
		google()
		mavenCentral()
	}
}

rootProject.name = "shikhi-android"
include(":app")
