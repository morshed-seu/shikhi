# Copy to keystore.properties (gitignored) and fill in to sign release builds.
# The keystore itself lives OUTSIDE the repository — never commit it (docs/70 §13).
storeFile=/absolute/path/to/shikhi-release.jks
storePassword=CHANGE_ME
keyAlias=shikhi
keyPassword=CHANGE_ME
